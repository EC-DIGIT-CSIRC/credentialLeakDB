sonar.projectKey=credentialLeakDB
sonar.organization=digits2
# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=credentialLeakDB
sonar.projectVersion=1.12.0
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
sonar.coverage.exclusions=doc/**,importer/**
sonar.exclusions=doc/**,tests/**,importer/**
# duplications exclusions
sonar.cpd.exclusions=doc/**
